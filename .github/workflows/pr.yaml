name: main
on:
  pull_request:
    branches:
    - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Get PR number
      run: |
        # TODO: Create a tag and a release
        # TODO: Create release notes
        # TODO: Deploy to prod
        # TODO: Send notifications
